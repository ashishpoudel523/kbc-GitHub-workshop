//MERN Stack small project
if (Response.data.id) {
    e.pretend
}




HomePage.js

    <
    center > < input type = "text"
class = "search-feild" > < /input> < /
center >

//
    .search - feild {

    }

mongo.exe mongodb://$[hostlist]/$[database]?authSource=$[authSource] --username $[username]

function App() {
    return {
        <
        Router >

        
        /Router>
    }
}


for (let b of data) {
    d.dataValues.image = urlConstants.IMAGE_PATH + d.dataValues.
}
async searchBooks() {
    const response = await api.get()
}
